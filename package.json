{
  "name": "minor-mars",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm run validate:peaks && npm run validate:seo-images && npm run validate:seo-meta && npx tsx scripts/prebuild-schema.ts",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "validate:peaks": "npx tsx scripts/validatePeaks.ts",
    "validate:seo-images": "npx tsx scripts/validateSeoImages.ts",
    "validate:seo-meta": "npx tsx scripts/validateSeoMeta.ts",
    "test": "npx tsx --test src/__tests__/schema.test.ts",
    "test:schema": "npm run test",
    "fetch-images": "node scripts/fetch-hero-images.js",
    "optimize-images": "node scripts/optimize-images.js",
    "fetch-weather-urls": "node scripts/apply-weather-urls.js"
  },
  "dependencies": {
    "astro": "^6.4.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "sharp": "^0.35.2",
    "tailwindcss": "^4.3.1"
  }
}
